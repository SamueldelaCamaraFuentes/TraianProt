% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{Diferential_boxplot}
\alias{Diferential_boxplot}
\title{Differential Protein Boxplot}
\usage{
Diferential_boxplot(df, metadata, protein, LOG2.names, selected_conditions)
}
\arguments{
\item{df}{A data frame with expression data. Must have a 'Protein' column
and LOG2 intensity columns.}

\item{metadata}{A data frame with sample metadata. Must have 'group' and
'log2_col' columns.}

\item{protein}{A character string specifying the single protein (from the
'Protein' column) to plot.}

\item{LOG2.names}{A character vector of all LOG2 sample column names to use.}

\item{selected_conditions}{A character vector of length 2 specifying the two
groups from the 'group' column to compare.}
}
\value{
This function is called for its side effect of printing a ggplot.
It invisibly returns the ggplot object \code{p}.
}
\description{
Creates a boxplot with jittered points for a single protein,
comparing two conditions.
}
\examples{
# Create synthetic data
df_box <- data.frame(
  Protein = "Prot1",
  LOG2.C1 = rnorm(1, 8), LOG2.C2 = rnorm(1, 8),
  LOG2.T1 = rnorm(1, 9), LOG2.T2 = rnorm(1, 9)
)
metadata_box <- data.frame(
  log2_col = c("LOG2.C1", "LOG2.C2", "LOG2.T1", "LOG2.T2"),
  group = c("Control", "Control", "Treatment", "Treatment")
)
log_names <- c("LOG2.C1", "LOG2.C2", "LOG2.T1", "LOG2.T2")
selected_cond <- c("Treatment", "Control")

# Generate the plot
if (requireNamespace("DOSE")) {
  Diferential_boxplot(df_box, metadata_box, "Prot1", log_names, selected_cond)
}

}
