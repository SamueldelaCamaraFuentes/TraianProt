% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{collapse_technical_replicates}
\alias{collapse_technical_replicates}
\title{Collapse Technical Replicates in Proteomics Data}
\usage{
collapse_technical_replicates(df, metadata)
}
\arguments{
\item{df}{A \code{data.frame} or \code{matrix} where rows are proteins/features and
columns are samples (intensities).}

\item{metadata}{A \code{data.frame} containing at least two columns:
\code{log2_col} (matching colnames in \code{df}) and \code{bioreplicate} (grouping ID).}
}
\value{
A \code{list} with two elements:
\itemize{
\item \code{data}: A \code{data.frame} with collapsed biological replicates as columns.
\item \code{metadata}: A \code{data.frame} with one row per biological replicate,
updated to match the new column names.
}
}
\description{
This function identifies technical replicates based on a biological
replicate metadata column and collapses them by calculating the mean intensity.
It updates both the expression data and the associated metadata.
}
\details{
The function assumes that technical replicates share the same
\code{sample_name} ID in the metadata. It cleans the column names by removing
trailing numeric indices (e.g., "Sample, 1" becomes "Sample") using regex.
If only one technical replicate exists for a biological group, it simply
renames the column.
}
\examples{
# Example metadata
meta <- data.frame(
    log2_col = c("S1, 1", "S1, 2", "S2, 1"),
    sample_name = c("Bio1", "Bio1", "Bio2"),
    intensity_sample_name = c("S1, 1", "S1, 2", "S2, 1"),
    stringsAsFactors = FALSE
)
# Example data
dat <- data.frame(
    `S1, 1` = c(1, 2), `S1, 2` = c(1.2, 2.2), `S2, 1` = c(5, 6),
    check.names = FALSE
)
result <- collapse_technical_replicates(dat, meta)
}
