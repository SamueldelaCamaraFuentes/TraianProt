% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{median_centering}
\alias{median_centering}
\title{Perform median centering normalization}
\usage{
median_centering(df, LOG2.names)
}
\arguments{
\item{df}{A numeric matrix where rows are proteins and columns are samples.}

\item{LOG2.names}{A vector containing the samples with log2 intensity values.}
}
\value{
A numeric matrix of the same dimensions, now median-centered.
}
\description{
Normalizes a numeric matrix of log-expression values by
subtracting the median of each column.
}
\examples{
\donttest{
# Create a small, synthetic log-expression matrix
log_df <- data.frame(
  Sample1 = c(10.0, 10.2, 9.8),
  Sample2 = c(12.0, 12.1, 11.9)
)
row.names(log_df) <- c("ProtA", "ProtB", "ProtC")

LOG2.names <- c("Sample1", "Sample2")

normalized_df <- median_centering(log_df, LOG2.names)

print(normalized_df)
}
}
