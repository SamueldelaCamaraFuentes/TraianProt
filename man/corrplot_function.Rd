% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{corrplot_function}
\alias{corrplot_function}
\title{Correlation Matrix}
\usage{
corrplot_function(
  df,
  metadata,
  display = "circle",
  tl.col = "black",
  addCoef.col = "black"
)
}
\arguments{
\item{df}{A data frame. The columns for which correlations are desired should
be numeric. Non-numeric columns will be ignored by \code{cor()}.}

\item{metadata}{A metadata data frame. Must contain 'group' and
'log2_col' columns to map samples to groups.}

\item{display}{A string specifying the visual representation (e.g., "circle").
\strong{Note:} This parameter is currently not implemented in the function body,
which defaults to 'tile'.}

\item{tl.col}{A string for text label color (i.e., the variable names).
\strong{Note:} This parameter is currently not implemented in the function body.}

\item{addCoef.col}{A string specifying the color for the overlaid
correlation coefficients (e.g., \code{"black"}).}
}
\value{
A \code{ggplot} object representing the correlation heatmap.
}
\description{
Calculates a Pearson correlation matrix from a data frame and visualizes it
as a tile-based heatmap using \code{ggplot2}, overlaying the correlation coefficients.
}
\details{
Generate a ggplot2 Correlation Matrix Heatmap

The function first calculates the correlation matrix using \code{cor()} with
\code{use = "complete.obs"}, which handles missing values by pairwise deletion.
It then melts the matrix into a "long" format suitable for \code{ggplot2}.
The plot uses \code{scale_fill_gradient2} to create a divergent color scale
(Red -> White -> Blue) centered at 0. \code{coord_fixed()} ensures square tiles.
}
\examples{
df_sample <- data.frame(
    LOG2.C1 = rnorm(20, mean = 10),
    LOG2.C2 = rnorm(20, mean = 10),
    LOG2.T1 = rnorm(20, mean = 12),
    LOG2.T2 = rnorm(20, mean = 12)
)

meta <- data.frame(
    group = c("Control", "Control", "Treatment", "Treatment"),
    log2_col = c("LOG2.C1", "LOG2.C2", "LOG2.T1", "LOG2.T2")
)

# 2. Generate the plot
p <- corrplot_function(df_sample, meta, addCoef.col = "blue")

# 3. Display the plot (Important: in examples we just call the object)
p
}
