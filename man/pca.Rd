% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{pca}
\alias{pca}
\title{Custom PCA Plot (Base R)}
\usage{
pca(x, metadata, selected_conditions)
}
\arguments{
\item{x}{A data frame or matrix with expression data. Columns are samples
(e.g., LOG2 intensity values). Rows are features (e.g. proteins).}

\item{metadata}{A data frame with sample metadata. Must have 'group' and
'log2_col' columns.}

\item{selected_conditions}{A character vector of length 2 specifying the two
groups from the 'group' column to compare.}
}
\value{
This function is called for its side effect of generating a base R
plot. It does not return an object.
}
\description{
Performs PCA and generates a 2D scatter plot of the first two
principal components using base R plotting.
}
\examples{
# Create synthetic data
x_data <- data.frame(
  LOG2.C1 = rnorm(10, 8), LOG2.C2 = rnorm(10, 8),
  LOG2.T1 = rnorm(10, 9), LOG2.T2 = rnorm(10, 9),
  row.names = paste0("Prot", 1:10)
)
metadata <- data.frame(
  log2_col = c("LOG2.C1", "LOG2.C2", "LOG2.T1", "LOG2.T2"),
  group = c("Control", "Control", "Treatment", "Treatment")
)
selected_cond <- c("Treatment", "Control") # Matches user's logic [2] then [1]

# Generate the plot
pca(x_data, metadata, selected_cond)

}
